# AI / Conversation Library

Curated notes imported from past AI chats by a librarian script, about 700
notes, one per conversation. Kept flat on purpose: the librarian rewrites every
note at its flat path, so sorting the folder by hand produced duplicates within
days. Never re-sort a folder an agent writes to; change the agent instead.
