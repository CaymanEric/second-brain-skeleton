# Mail

Everything pulled out of email, one subfolder per source type, each with its
own index note and a naming rule. `Mail.md` lists the folders and which sender
addresses feed each one.

Agents never read this folder unless the request is about mail.
